<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fancky.authorization.mapper.SysUserMapper">

    <resultMap id="userResultMap" type="com.fancky.authorization.model.entity.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="enabled" column="enabled"/>
        <result property="accountNonExpired" column="account_non_expired"/>
        <result property="accountNonLocked" column="account_non_locked"/>
        <result property="credentialsNonExpired" column="credentials_non_expired"/>
        <result property="lastLoginTime" column="last_login_time"/>
<!--        <result property="deleted" column="deleted"/>-->
        <result property="password_last_change_time" column="password_last_change_time"/>
        <result property="createTime" column="password_expire_days"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
<!--        <collection property="roles" ofType="com.fancky.authorization.model.entity.SysRole">-->
<!--            <id property="id" column="role_id"/>-->
<!--            <result property="roleCode" column="role_code"/>-->
<!--            <result property="roleName" column="role_name"/>-->
<!--            <result property="status" column="role_status"/>-->
<!--        </collection>-->
    </resultMap>

    <select id="selectUserByUsername" resultMap="userResultMap">
        SELECT
            u.*
--                ,
--             r.id as role_id,
--             r.role_code,
--             r.role_name,
--             r.status as role_status
        FROM sys_user u
                 LEFT JOIN sys_user_role ur ON u.id = ur.user_id AND ur.deleted = 0
                 LEFT JOIN sys_role r ON ur.role_id = r.id AND r.deleted = 0
        WHERE u.username = #{username} AND u.deleted = 0
    </select>

    <select id="selectUserList" resultMap="userResultMap">
        SELECT
        u.*,
        r.role_code,
        p.permission_code
        FROM sys_user u
        LEFT JOIN sys_user_role ur ON u.id = ur.user_id
        LEFT JOIN sys_role r ON ur.role_id = r.id
        LEFT JOIN sys_role_permission rp ON r.id = rp.role_id
        LEFT JOIN sys_permission p ON rp.permission_id = p.id
        <where>
            u.deleted = 0
            <if test="ew != null and ew.sqlSegment != null">
                AND ${ew.sqlSegment}
            </if>
        </where>
        ORDER BY u.create_time DESC
    </select>
</mapper>